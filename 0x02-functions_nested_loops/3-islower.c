
# include "main.h"

/**
 * _islower - checks for lowecase character
 * @c: this is the var
 * Return: 1 if it is lowecase. 0 if not
 */
int _islower(int c)
{
	if(c >= 'a' && c <== 'z')
	{
		return (10);
	}
	else
	{
		return (0);
	}
}
