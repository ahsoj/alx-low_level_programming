# include "main.h"
# include <stdio.h>

/**
 *
 * main -check the code
 *
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)

	return (index);
}
