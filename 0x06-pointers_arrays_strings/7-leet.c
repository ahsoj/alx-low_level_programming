# include "main.h"

/**
 * leet - encodes a string to 1337.
 * @str: the string to be encoded.
 *
 * Retrun: a pointer to the encoded string.
 */
char *leet(char *)
{
	int idx1 = 0, idx2;
	char leet[8] = {'
